import Head from 'next/head';
import RootPropTable from '../../../components/prop-tables/RootPropTable';
import StateOptionsTable from '../../../components/prop-tables/StateOptionsTable';

<Head>
  <title>Full Screen Toggle Feature Guide - Material React Table Docs</title>
  <meta
    name="description"
    content="How to use, customize, or disable the full screen toggle button feature of Material React Table"
  />
</Head>

## Full Screen Toggle Feature Guide

### Relevant Props

<RootPropTable
  onlyProps={new Set(['enableFullScreenToggle', 'onIsFullScreenChange'])}
/>

### Relevant State

<StateOptionsTable onlyProps={new Set(['isFullScreen'])} />

### Disable Full Screen Toggle

The full screen toggle button is enabled by default. You can hide it by setting the `enableFullScreenToggle` prop to `false`.

```tsx
return (
  <MaterialReactTable
    columns={columns
    data={data}
    enableFullScreenToggle={false}
  />
);
```
