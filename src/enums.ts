export enum MRT_FILTER_TYPE {
  CONTAINS = 'contains',
  EMPTY = 'empty',
  ENDS_WITH = 'endsWith',
  EQUALS = 'equals',
  FUZZY = 'fuzzy',
  GREATER_THAN = 'greaterThan',
  LESS_THAN = 'lessThan',
  NOT_EMPTY = 'notEmpty',
  NOT_EQUALS = 'notEquals',
  STARTS_WITH = 'startsWith',
}
