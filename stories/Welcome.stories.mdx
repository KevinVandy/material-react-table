import { Meta, Story } from '@storybook/addon-docs';
import { Meta, Story } from '@storybook/react';
import { ReactTableMui, ReactTableMuiProps } from '../src';
import faker from '@faker-js/faker';

<Meta title="Table Sections" component={ReactTableMui} />;

export const Template: Story<ReactTableMuiProps> = (args) => (
  <ReactTableMui {...args} />
);

<Story name="Simple" args={columns: [
    {
      Header: 'First Name',
      accessor: 'firstName',
    },
    {
      Header: 'Last Name',
      accessor: 'lastName',
    },
  ],
  data: [...Array(5)].map((_) => ({
    firstName: faker.name.firstName(),
    lastName: faker.name.lastName(),
  })),}>
  {Template.bind({})}
</Story>;
