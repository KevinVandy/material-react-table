import Head from 'next/head';
import Example from '../../../examples/react-query';

<Head>
  <title>Material React Table Docs - React Query Remote Example</title>
  <meta
    name="description"
    content="An example of Material React Table which shows how to fetch remote data and filter and paginate on a remote server with react-query"
  />
</Head>

## React Query (Remote) Example

This is just like the [Remote Data Example](/docs/examples/remote), but react-query is used to simplify all of the state management of the fetching and loading of data.

Also be sure to check out the [Virtualized Example](/docs/examples/virtualized) which shows off the use of another TanStack library, TanStack React Virtual, to render thousands of rows at once, but still maintain great performance.

<Example />

View Extra Storybook **[Examples](https://www.material-react-table.dev/?path=/story/prop-playground--default)**
