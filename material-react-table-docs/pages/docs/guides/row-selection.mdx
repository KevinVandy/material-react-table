import Head from 'next/head';
import EnableExample from '../../../examples/enableRowSelection';
import CustomizeExample from '../../../examples/customizeRowSelection';

<Head>
  <title>Material React Table Docs - Selection Feature Guide</title>
</Head>

## Selection Feature Guide

### Relavent Props

- autoResetSelectedRows
- enableSelectAll
- enableRowSelection
- initialState.selectedRowIds
- manualRowSelectedKey
- muiSelectCheckboxProps
- onHandleSelectAllChange
- onHandleSelectRowChange

---

### Enable Selection

Selection checkboxes can be enabled with the `enableRowSelection` prop.

<EnableExample />

### Disable Select All

By default, if you enable selection for each row, there will also be a select all checkbox in the header of the checkbox column. It can be hidden with the `enableSelectAll` prop.

```jsx
<MaterialReactTable
  data={data}
  columns={columns}
  enableSelectAll={false}
  enableRowSelection
/>
```

### Customize Select Checkboxes

The selection checkboxes can be customized with the `muiSelectCheckboxProps` prop. Any prop that can be passed to a Mui Checkbox component can be specified here. For example, you may want to use a different color for the checkbox, or use some logic to disable certain rows from being selected.

```jsx
<MaterialReactTable
  data={data}
  columns={columns}
  enableRowSelection
  muiSelectCheckboxProps={{
    color: 'secondary',
  }}
/>
```

### Selection Callback Functions

There are two callbacks that can be used to respond to selection changes. `onHandleSelectRowChange` is called when a row is selected or deselected. `onHandleSelectAllChange` is called when the select all checkbox is selected or deselected.

<CustomizeExample />
